{
  "name": "zlib",
  "version": "1.3.1",
  "port-version": 100,
  "description": "MeshLib overlay: replaces vcpkg's stock zlib with zlib-ng built in compat mode (ABI-compatible libz). Downstream consumers still see zlib.h, libz, and the ZLIB::ZLIB CMake target; compression and decompression are handled by zlib-ng's faster implementations.",
  "homepage": "https://github.com/zlib-ng/zlib-ng",
  "license": "Zlib",
  "dependencies": [
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ]
}
