set(MBEDTLS_FOUND 1)
set(MBEDTLS_LIBRARY "${CMAKE_CURRENT_SOURCE_DIR}/../mbedtls/library")
set(MBEDTLS_INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../mbedtls/include")
set(MBEDTLS_VERSION "3.5.0")
set(MBEDTLS_VERSION_MAJOR 3)
set(MBEDTLS_VERSION_MINOR 5)
set(MBEDTLS_VERSION_PATCH 0)
set(MBEDTLS_VERSION_TWEAK 0)
set(MBEDTLS_VERSION_COUNT 3)
add_library(MbedTLS::MbedTLS UNKNOWN IMPORTED)
set_target_properties(MbedTLS::MbedTLS PROPERTIES
  IMPORTED_LOCATION "${MBEDTLS_LIBRARY}"
  INTERFACE_INCLUDE_DIRECTORIES "${MBEDTLS_INCLUDE_DIR}"
)
