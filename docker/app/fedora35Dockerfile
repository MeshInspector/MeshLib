FROM fedora:35 AS build

ARG FILE_NAME
ARG FILE_URL

RUN dnf -y update && \
    dnf -y install sudo python3-pip wget boost-devel && \
    wget -O /tmp/${FILE_NAME} ${FILE_URL} && \
    rpm -ivh /tmp/${FILE_NAME} && \
    rm /tmp/${FILE_NAME} && \
    dnf clean all
