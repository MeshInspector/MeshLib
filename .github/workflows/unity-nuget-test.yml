name: Test Nuget package with Unity

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag to test'
        required: true
        type: string
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string

# `contents: write` is required to see the release: pr-test releases created
# by full-ci PR runs are drafts, and drafts are invisible to a read-only token
# ("release not found" from `gh release download`).
permissions:
  contents: write

# Serialize whole runs; within a run each job activates a personal license
# on start and returns it when the job ends (professional seats are capped
# at 2 concurrent activations, personal ones are not).
concurrency:
  group: unity-test
  cancel-in-progress: false

jobs:
  unity-nuget-test:
    name: Unity test (${{ matrix.os }})
    # Job-level timeout is a safety net (editor download + install alone
    # takes ~10 minutes); the real cap for the editor run is on the
    # "Run test" step below so that a timeout there is reported as a
    # `failure` rather than `cancelled`, letting the results upload execute.
    timeout-minutes: 60
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        os: [ windows-latest, ubuntu-latest, macos-latest, macos-15-intel ]
    runs-on: ${{ matrix.os }}
    # Windows is the established release gate; the other platforms exercise
    # the previously untested linux-x64 / osx-arm64 / osx-x64 natives of the
    # package and stay non-blocking until proven stable.
    continue-on-error: ${{ matrix.os != 'windows-latest' }}
    env:
      UNITY_PROJECT: test_unity/NugetTestProj
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # The job token has `contents: write`; don't leave it readable in
          # .git/config for later steps.
          persist-credentials: false

      - name: Download nuget package
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release download ${{ inputs.release_tag }} --pattern "*.nupkg" --repo ${{ github.repository }} --clobber

      - name: Put package into local NuGet feed
        shell: pwsh
        run: |
          $pkg = Get-ChildItem -Filter "MeshLib*.nupkg" | Select-Object -First 1
          if (!$pkg) {
            Write-Error "No MeshLib*.nupkg file found"
            exit 1
          }
          $matches = $null
          $foundVersion = $pkg.Name -match '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+'
          if (!$foundVersion) {
            Write-Error "Cannot extract version from MeshLib*.nupkg"
            exit 1
          }
          $pkgVersion = $matches[0]

          $feedDir = Join-Path $env:GITHUB_WORKSPACE "$env:UNITY_PROJECT/nuget_repo"
          New-Item -ItemType Directory -Force -Path $feedDir | Out-Null
          Move-Item $pkg.FullName (Join-Path $feedDir "MeshLib.$pkgVersion.nupkg")

          # NuGetForUnity's resolution base for relative feed paths is not
          # well defined, so point the local package source at the feed
          # folder by absolute path.
          $patched = $false
          foreach ($cfg in Get-ChildItem -Path $env:UNITY_PROJECT -Recurse -Filter "NuGet.config") {
            $xml = [xml](Get-Content -Path $cfg.FullName)
            foreach ($src in $xml.configuration.packageSources.add) {
              if ($src.key -eq 'LocalMeshLib' -or $src.value -match 'nuget_repo') {
                $src.value = "$feedDir"
                $patched = $true
              }
            }
            $xml.Save($cfg.FullName)
          }
          if (!$patched) {
            Write-Error "No NuGet.config with a 'LocalMeshLib' (or *nuget_repo*) package source found under $env:UNITY_PROJECT"
            exit 1
          }

          $xmlPath = Join-Path $env:GITHUB_WORKSPACE "$env:UNITY_PROJECT/Assets/packages.config"
          $xml = [xml](Get-Content -Path $xmlPath)
          $xml.packages.package.version = $pkgVersion
          $xml.Save($xmlPath)

      - name: Install Unity editor
        uses: buildalon/unity-setup@30fcbcb56c10ea5d64298e970d952b8d29bc268b # v2.6.0
        with:
          version-file: ${{ env.UNITY_PROJECT }}/ProjectSettings/ProjectVersion.txt

      - name: Activate Unity license
        # Personal licenses can no longer be activated from a manual .ulf
        # file (license.unity3d.com/manual only offers serial activation
        # now), so this drives Unity's own licensing client instead -- the
        # same official binary Unity Hub uses. The account credentials only
        # pass through this first-party step, never through a third-party
        # action. The account must have accepted the Personal license terms
        # once (Unity Hub -> Licenses -> Get a free personal license) and
        # must not have 2FA enabled.
        shell: pwsh
        env:
          UNITY_USERNAME: ${{ secrets.UNITY_USERNAME }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        run: |
          if (-not $env:UNITY_USERNAME -or -not $env:UNITY_PASSWORD) {
            Write-Error "UNITY_USERNAME / UNITY_PASSWORD secrets are not set"
            exit 1
          }
          if ($IsMacOS) {
            sudo mkdir -p "/Library/Application Support/Unity"
            sudo chown -R "$env:USER" "/Library/Application Support/Unity"
          }
          $clientName = $IsWindows ? "Unity.Licensing.Client.exe" : "Unity.Licensing.Client"
          $editorDir = Split-Path $env:UNITY_EDITOR_PATH
          $client = @(
            (Join-Path $editorDir "Data/Resources/Licensing/Client/$clientName"),
            (Join-Path (Split-Path $editorDir) "Frameworks/UnityLicensingClient_V1.app/Contents/MacOS/$clientName")
          ) | Where-Object { Test-Path $_ } | Select-Object -First 1
          if (-not $client) {
            $client = (Get-ChildItem -Path $editorDir, (Split-Path $editorDir) -Recurse -Filter $clientName -File -ErrorAction SilentlyContinue | Select-Object -First 1).FullName
          }
          if (-not $client) {
            Write-Error "Unity.Licensing.Client not found near $env:UNITY_EDITOR_PATH"
            exit 1
          }
          Write-Output "Licensing client: $client"
          "UNITY_LICENSING_CLIENT=$client" >> $env:GITHUB_ENV
          & $client --activate-ulf --username $env:UNITY_USERNAME --password $env:UNITY_PASSWORD --include-personal
          exit $LASTEXITCODE

      - name: Prime Unity project
        # First cold start on a fresh runner: resolves the NuGetForUnity UPM
        # package (git dependency) and compiles its asset postprocessor into
        # Library BEFORE the nuget natives are restored. Without this, native
        # plugins imported during the first-ever editor session keep the
        # restorer's placeholder import settings (all platforms disabled) and
        # fail with DllNotFoundException. The exit code is not checked: this
        # session reports script compile errors because MRDotNet2.dll is not
        # restored yet, which is expected and harmless.
        timeout-minutes: 15
        shell: pwsh
        run: |
          Start-Process -FilePath $env:UNITY_EDITOR_PATH -ArgumentList "-batchmode","-nographics","-quit","-projectPath",(Join-Path $env:GITHUB_WORKSPACE $env:UNITY_PROJECT),"-logfile",(Join-Path $env:GITHUB_WORKSPACE "unity-prime.log") -Wait -NoNewWindow

      - name: Setup .NET
        # macOS runners ship dotnet on PATH but without a registered install
        # location, so the NuGetForUnity.Cli tool host fails with ".NET
        # location: Not found"; setup-dotnet registers it (sets DOTNET_ROOT).
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '9.x'

      - name: Install nuget package
        # Runs after the prime pass so the natives restored here are imported
        # by an editor session that already has the postprocessor compiled.
        shell: pwsh
        run: |
          dotnet tool install --global NuGetForUnity.Cli
          nugetforunity restore "$env:UNITY_PROJECT"

      - name: Run test
        # Step-level timeout: when the Unity Editor invocation exceeds this
        # cap, the step ends with `failure` (not `cancelled`) and the result
        # and log printing below still execute. `-runTests` exit codes:
        # 0 all tests passed, 2 tests failed, 3 run error.
        timeout-minutes: 20
        shell: pwsh
        run: |
          $unityArgs = @(
            "-batchmode", "-nographics",
            "-projectPath", (Join-Path $env:GITHUB_WORKSPACE $env:UNITY_PROJECT),
            "-runTests", "-testPlatform", "playmode",
            "-testResults", (Join-Path $env:GITHUB_WORKSPACE "results.xml"),
            "-logfile", (Join-Path $env:GITHUB_WORKSPACE "unity-test.log")
          )
          $proc = Start-Process -FilePath $env:UNITY_EDITOR_PATH -ArgumentList $unityArgs -Wait -PassThru -NoNewWindow
          Write-Output "Unity exit code: $($proc.ExitCode)"
          exit $proc.ExitCode

      - name: Print test results
        # Job logs live as long as artifacts (same retention setting), so the
        # parsed results are printed instead of uploaded. Informational only:
        # pass/fail gating stays with the Run test exit code.
        if: always()
        shell: pwsh
        run: |
          if (-not (Test-Path results.xml)) {
            Write-Output "No results.xml produced (the editor failed before running tests); see the editor log below."
            exit 0
          }
          [xml]$r = Get-Content results.xml
          $tr = $r.'test-run'
          Write-Output "Test run result: $($tr.result) (total=$($tr.total) passed=$($tr.passed) failed=$($tr.failed) skipped=$($tr.skipped) duration=$($tr.duration)s)"
          foreach ($tc in $r.SelectNodes("//test-case")) {
            Write-Output "  $($tc.fullname): $($tc.result) ($($tc.duration)s)"
            if ($tc.result -eq "Failed") {
              $msg = $tc.failure.message.'#cdata-section'
              $stack = $tc.failure.'stack-trace'.'#cdata-section'
              if ($msg) { Write-Output "    message: $($msg.Trim())" }
              if ($stack) { Write-Output "    stack: $($stack.Trim())" }
              $oneLine = ("$($tc.fullname): $msg" -replace "\s+", " ").Trim()
              Write-Output "::error title=Unity test failed::$oneLine"
            }
          }

      - name: Print editor log
        if: always()
        shell: pwsh
        run: Get-Content unity-test.log -ErrorAction SilentlyContinue

      - name: Return Unity license
        if: always()
        shell: pwsh
        run: |
          if ($env:UNITY_LICENSING_CLIENT) {
            & $env:UNITY_LICENSING_CLIENT --return-ulf
          }
