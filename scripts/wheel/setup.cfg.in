[metadata]
version = $VERSION

[options]
packages = meshlib
include_package_data = True

[options.package_data]
meshlib = $PACKAGE_DATA
share =
    MRDarkTheme.json
    MRLightTheme.json

[bdist_wheel]
python_tag = $PYTHON_TAG
plat_name = $PLAT_NAME
