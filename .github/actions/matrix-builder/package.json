{
  "name": "matrix-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Declarative ordered include/extend/exclude rules for GitHub Actions matrices",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --minify",
    "test": "node --test tests/engine.test.js"
  },
  "dependencies": {
    "@actions/core": "^2.0.3",
    "js-yaml": "^4.2.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
